name: On push, deploying to Test
on: [push]
jobs:
  run-build-and-deploy:
    uses: octopusden/octopus-base/.github/workflows/common-py-2-3-build-deploy.yml@main
    with:
      process_env: Test
    secrets: inherit
